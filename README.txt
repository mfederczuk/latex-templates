Personal LaTeX Templates

Use '%%' in template files for template-only comments, these will be stripped when copying the template. (when an entire
line is just a template-only comment, the entire line will be deleted)
